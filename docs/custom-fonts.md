# Custom Fonts

As of `v3.1.0`, MissingCore Music supports custom fonts. The fonts you import can be used as the accent or primary font.

- We only support `.otf` & `.ttf` fonts. Font weights are weird on Android, so import font files with the wanted weight.
- There's a chance of some weird UI issues caused by using custom fonts (ie: missing text, clipped content).

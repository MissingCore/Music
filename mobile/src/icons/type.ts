export type Icon = {
  /** Defaults to `24px`. */
  size?: number;
  /** Defaults to theme's `foreground` color. */
  color?: string;
  /**
   * Use the `filled` variant on the icon if available.
   * Defaults to `false`.
   */
  filled?: boolean;
};
